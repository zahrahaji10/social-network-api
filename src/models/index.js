// import models
const User = require("./User");
const Reaction = require("./Reaction");
const Thought = require("./Thought");

// export the models
module.exports = {
  User,
  Reaction,
  Thought,
};
